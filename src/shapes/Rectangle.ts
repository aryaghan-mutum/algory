/**
 * Get rectangle area
 * @param length
 * @param width
 */
export const rectangleArea = (length: number, width: number): number => length * width

/**
 * Get rectangle Perimeter
 * @param length
 * @param width
 */
export const rectanglePerimeter = (length: number, width: number): number => 2 * (length + width)
