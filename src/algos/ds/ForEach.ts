import { isEmpty } from '../lc/ListComputation'
// FIXME: 
export const forEach = (arr: []): any => {

    const forEachIter = (arr: [], acc: null) => {
        if(isEmpty(arr)) {
            return arr 
        } 

    }

    forEachIter(arr, null)
}
